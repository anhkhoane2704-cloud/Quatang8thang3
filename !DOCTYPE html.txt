<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>8/3 - Gửi các bạn nữ 11A5</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 99%, #fecfef 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden;
        }

        /* Bì thư ban đầu */
        .card-container {
            position: relative;
            cursor: pointer;
            transition: transform 0.3s ease;
            z-index: 10;
        }

        .card-container:hover {
            transform: scale(1.05);
        }

        .envelope {
            background: #fff;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 15px 30px rgba(0,0,0,0.15);
            text-align: center;
            border: 3px solid #ffb6c1;
            min-width: 300px;
        }

        .envelope h1 {
            color: #ff4d6d;
            margin: 10px 0 0 0;
            font-size: 22px;
        }

        /* Nội dung lời chúc bên trong */
        .message-content {
            display: none; /* Ẩn đi lúc đầu */
            background: rgba(255, 255, 255, 0.95);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            max-width: 600px;
            width: 85%;
            box-shadow: 0 20px 40px rgba(255, 77, 109, 0.2);
            animation: fadeIn 0.8s ease forwards;
            z-index: 20;
        }

        .message-content h2 {
            color: #ff4d6d;
            font-size: 28px;
            margin-bottom: 20px;
        }

        .message-content p {
            color: #444;
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
</head>
<body>

    <div class="card-container" id="envelope" onclick="openCard()">
        <div class="envelope">
            <span style="font-size: 80px; display: block;">💌</span>
            <h1>Có một bức thư gửi 11A5...</h1>
            <p style="color: #888; font-size: 14px; margin-top: 10px;">(Nhấn vào để mở)</p>
        </div>
    </div>

    <div class="message-content" id="message">
        <h2>🌸 Chúc mừng ngày 8/3! 🌸</h2>
        <p>Nhân ngày Quốc tế Phụ nữ, chúc tất cả các bạn nữ lớp 11A5 luôn xinh đẹp, rạng rỡ và tràn ngập niềm vui.</p>
        <p>Cảm ơn các bạn vì đã luôn là những mảnh ghép tuyệt vời nhất của tập thể lớp chúng ta. Chúc các bạn một ngày 8/3 thật đáng nhớ!</p>
        <p style="font-weight: bold; color: #ff4d6d; margin-top: 30px; font-size: 20px;">- Từ các bạn nam 11A5 -</p>
    </div>

    <script>
        function openCard() {
            // 1. Ẩn bì thư, hiện lời chúc
            document.getElementById('envelope').style.display = 'none';
            document.getElementById('message').style.display = 'block';

            // 2. Kích hoạt hiệu ứng pháo hoa
            var duration = 3 * 1000; // Pháo hoa nổ trong 3 giây
            var animationEnd = Date.now() + duration;
            var defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 };

            function randomInRange(min, max) {
                return Math.random() * (max - min) + min;
            }

            var interval = setInterval(function() {
                var timeLeft = animationEnd - Date.now();
                if (timeLeft <= 0) {
                    return clearInterval(interval);
                }
                var particleCount = 50 * (timeLeft / duration);
                // Bắn từ 2 bên mép màn hình
                confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 } }));
                confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 } }));
            }, 250);
        }
    </script>
</body>
</html>